{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go run midi/cmd/usb-piano",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }

    ]
}
