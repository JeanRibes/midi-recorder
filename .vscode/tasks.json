{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go run ./cmd/piano-assistant",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }

    ]
}
